# Toolbar紧凑入口

- 问题：自定义 Toolbar 多按钮横向铺开后会覆盖 Unity 原生中间 Play/Pause/Step 按钮。
- 处理：将主 Toolbar 可见区压缩为单个 `GDK v` 入口，并移动到 AI 后方空位；原 Scene、ET、Excel、Open、Compile 等命令统一收纳到菜单内。
- 验证：Unity 编译通过且 Console 无 Error；截图确认 Unity 原生中间按钮已恢复可见，`GDK v` 不再覆盖 Play/Pause/Step。
