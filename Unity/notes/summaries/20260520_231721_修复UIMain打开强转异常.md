# 任务总结

- 时间：2026-05-20 23:17:21
- 主题：修复 `UIMain` 打开时的 `InvalidCastException`
- 根因：`Assets/Scripts/Game/ET/Code/HotfixView/Client/GenAtom/UI/UIMain/SceneChangeFinish_CreateUIMain.cs` 虽然文件名和类名是 `CreateUIMain`，但实际仍在创建 `UIFormSkillComponent` 并打开 `UGFUIFormId.UIMain`，导致 `MonoUIFormMain` 被交给 `UIFormSkill` 泛型链路，在 `UGFUIForm<T>.UGFMono` 强转时抛出异常
- 修复：已将入口改为移除旧的 `UIFormSkillComponent`，并通过 `AddUIFormComponentAsync<UIFormMain>(UGFUIFormId.UIMain)` 正确创建 `UIFormMain`
- 验证：已执行 AIBridge `focus --raw`、`editor stop --raw`、`compile unity --raw --timeout 120000`，编译结果 `errorCount: 0`、`warningCount: 0`
- 注意：当前 `UIMain` 相关目录仍有多处未提交新增文件，本次未自动提交，避免混入你工作区中的其他调整
