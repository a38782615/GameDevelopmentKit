# 任务总结

- 时间：2026-05-20 21:42:03
- 主题：补充 CodeBind 绑定规则到 AGENTS
- 结果：已在 `AGENTS.md` 中补充 Generate Bind Code 的 `_` 分隔符命名规则、类型名来源、数组命名约束、嵌套绑定边界与推荐操作步骤。
- 关键说明：`UI_Main` 这类名称不会直接形成有效绑定字段，真实可绑定节点必须满足 `变量名_类型名` 规则，例如 `Map_ExButton`。
- 本次未改动 `UIMain` prefab 与业务脚本，仅沉淀规范。
