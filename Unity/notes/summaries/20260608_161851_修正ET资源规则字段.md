# 修正 ET 资源规则字段

## 修改内容

- 修正 `ResourceRuleEditor_ET.asset` 的序列化字段名。
- 将旧字段 `isActivate`、`rules`、`valid`、`name` 等迁移为当前 `ResourceRuleEditorData` / `ResourceRule` 使用的 `m_IsActivate`、`m_Rules`、`m_Valid`、`m_Name` 等字段。
- 保留原有规则内容和激活状态，不调整规则路径、筛选类型或搜索模式。

## 验证

- 已确认 `ResourceRuleEditor_ET.asset` 中不再存在旧字段名。
- Unity 编译通过：错误 0，警告 0。
- 已检查本次涉及文件中文编码：未发现乱码特征。
