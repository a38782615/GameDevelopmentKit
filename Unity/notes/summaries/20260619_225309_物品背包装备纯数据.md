# 物品背包装备纯数据

## 修改内容

- 新增 `InventoryData` 纯数据结构，包含背包和装备槽映射。
- 新增 `InventoryDataComponent` 及其存档加载/保存系统。
- 将 `InventoryData` 挂到 `GameDataMgrComponent` 的统一数据管理链路里。
- 基于 `DTItems` 增加物品配置访问扩展，并限制装备仅允许 `ItemType = 2`。
- 补回缺失的 `ItemType.cs`，恢复模型程序集编译。

## 验证

- 已执行 Unity 编译检查，结果通过。