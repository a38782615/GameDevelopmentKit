# PlayerData Age 修正

- `PlayerDataComponent` 加载时，如果 `Age == 0`，自动修正为 `16` 并写回存档。
- 加载完成后输出日志，包含 `Age`、`Level`、`Exp`、`NickName`。
- 默认新建 `PlayerData` 时直接使用 `Age = 16`。
- 使用 AIBridge 执行 `focus`、`editor stop`、`compile unity --raw --timeout 120000`，Unity 编译通过，0 error / 0 warning。
