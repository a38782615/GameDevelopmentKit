# 玩家技能关联字段

## 完成内容

- `PlayerData` 增加技能经验 `SkillExp`。
- `PlayerSkillData` 增加 `PlayerId` 和 `ConfigId`；`Id` 调整为独立存档记录主键。
- `PlayerId` 使用 `long` 并关联当前 `PlayerData.Id`。
- 技能索引、`DTSkill` 查询和 `DTSkillAttribute` 升级判断统一使用 `ConfigId`。
- 加载旧存档时，使用旧记录的 `Id` 回填 `ConfigId`，并补齐当前玩家 Id。
- 消除 `GameDataMgrComponentSystem` 与 `PlayerSkillDataComponentSystem` 的静态调用环。

## 验证

- Unity 编译通过：0 个错误，0 个警告。
