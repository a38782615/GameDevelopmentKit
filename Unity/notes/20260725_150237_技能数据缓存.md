# 技能数据缓存

## 完成内容

- 将玩家已学技能列表替换为 `XList`。
- 添加按技能 Id 的 `XDictionary` 索引，避免查询时线性查找。
- 添加已装备、已装备主动、已装备被动和可升级技能的 `XList` 缓存。
- 在加载、学习、切换装备状态和升级后重建缓存；主动与被动严格按 `DTSkill.IsAct` 分类。

## 验证

- Unity 编译通过：0 个错误，0 个警告。
