# 英雄初始技能存档

## 完成内容

- `LoadPlayerSkillData` 查询结果为空时读取当前玩家对应的 `DTHero.Skill`。
- 将有效初始技能按等级 `0`、已装备状态写入 `PlayerSkillData` 并保存。
- 无效技能配置会跳过并记录警告，不阻断其他技能初始化。
- 英雄运行时技能授予适配统一 `Skill` 数组，并按 `DTSkill.IsAct` 区分主动和被动技能。

## 验证

- Unity 编译通过：0 个错误，0 个警告。
