# CodexAdmin同步当前设置

## 修改内容

- 将 Unity 工具栏 `CodexAdmin` 的启动参数同步为当前 Codex 配置：`gpt-5.6-sol`、`model_reasoning_effort=medium`、`service_tier=default`。
- 保留原有 `danger-full-access` 沙箱和 `never` 审批策略。
- 同步更新工具栏按钮提示，确保显示内容与实际启动参数一致。

## 验证

- 已在退出 Unity Play 模式后执行 AIBridge `compile unity --raw --timeout 120000`，结果为 0 错误、0 警告。
- 已通过 AIBridge 检查 Unity Console Error，未发现错误日志。
- 已执行 `codex --model gpt-5.6-sol --config model_reasoning_effort=medium --config service_tier=default --sandbox danger-full-access --ask-for-approval never --version`，CLI 参数解析正常。
