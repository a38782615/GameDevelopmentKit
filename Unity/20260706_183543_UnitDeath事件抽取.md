# UnitDeath 事件抽取

## 修改内容

- 新增 `UnitDeath` 事件，事件数据只携带死亡的 `Unit`。
- 将死亡动画等待、掉落物生成、单位移除逻辑迁移到 `UnitDeath_PlayDeathPresentationAndRemove.cs`。
- `AbilitySystemComponentSystem.PlayDeathPresentationAndRemove` 收缩为发布 `UnitDeath` 事件的薄封装。
- 保留原有受击表现逻辑，避免影响 `OnTakeDamage` 流程。

## 验证

- 已执行行尾空白检查，未发现问题。
- 已检查本次代码改动，没有新增中文注释或中文字符串编码风险。
- 已通过 AIBridge 执行 Unity 编译，结果 `errorCount=0`、`warningCount=0`。

## 备注

- 本次未执行 git 提交。
