# CodexAdmin启动配置

## 修改内容

- 将 Unity 工具栏 `CodexAdmin` 启动参数固定为 `gpt-5.5`、`model_reasoning_effort=xhigh`、`service_tier=default`。
- 保留原有 `danger-full-access` 沙盒和 `never` 审批策略。
- 更新工具栏按钮提示，避免显示信息与实际启动参数不一致。

## 验证

- 已检查用户级 Codex 配置，当前已经是 `gpt-5.5 / xhigh / default`。
- 已通过 AIBridge `compile unity --raw --timeout 120000` 验证，错误 0、警告 0。
- 已执行 `codex --model gpt-5.5 --config model_reasoning_effort=xhigh --config service_tier=default --version`，CLI 参数解析正常。
