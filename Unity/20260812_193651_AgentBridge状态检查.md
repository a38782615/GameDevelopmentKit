# AgentBridge 状态检查

## 检查结果

- Unity Editor 正在运行，版本为 `6000.3.17f1`。
- 新版 `me.xw.unityagentbridge` 包已安装，版本为 `2.0.0`。
- `Library/ScriptAssemblies/AgentBridge.Editor.dll` 已成功生成，说明包本身已完成编译。
- 项目根目录不存在 `.agentbridge`，因此新版 Agent Bridge 宿主当前未启用。
- `AIBridgeCache/CLI/AIBridgeCLI.exe` 不存在，无法使用旧版 AIBridgeCLI 命令。
- `ProjectSettings/AIBridgeSettings.asset` 是旧版 `cn.lys.aibridge` 的残留配置；对应包和 CLI 当前未安装。

## 恢复步骤

1. 在 Unity 中打开 `Window/Agent Bridge`。
2. 点击 `启用桥接`，由插件创建项目根目录下的 `.agentbridge` 并启动宿主。
3. 需要 Unity 失焦后继续接收请求时，同时启用 `后台运行`。
4. `.agentbridge` 出现后，通过协议首先调用 `list_commands` 获取当前运行时命令集。

根据 Agent Bridge 协议，AI 不应自行创建 `.agentbridge`，因此本次仅检查状态，没有修改桥接配置。
