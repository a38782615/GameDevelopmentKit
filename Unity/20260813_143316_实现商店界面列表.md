# 实现商店界面列表

## 修改内容

- 在 `UIFormShopSystem` 中接入 `Shop_LoopHorizontalScrollRect`，根据当前页签刷新商品数量并渲染名称、数量和图标状态。
- `ShopTabs_UXToggleGroup` 的 Tab0 显示 `Medicine`，Tab1 暂时显示空列表，Tab2 显示 `Herb`。
- 新增 `UIWidgetShopItem`、`MonoUIShopItem` 及对应系统，并通过 CodeBind 生成名称、数量和图标绑定字段。
- 在 `UIFormShop.prefab` 的循环列表模板上挂载 `MonoUIShopItem`，规范绑定节点名称并移除无关拖拽组件。
- 使用 UIForm 内部注册表维护循环列表实例与 ET Widget 的对应关系，未使用 Transform 节点查找补充绑定。

## 验证

- 已停止 Unity Play Mode，并执行 AIBridge `focus --raw`。
- 已通过 AIBridge `compile unity --raw --timeout 120000`，结果为 0 错误、0 警告。
- 已通过 AIBridge `get_logs --logType Error --raw --timeout 30000` 检查 Unity Console，Error 日志为空。
- 已确认 `MonoUIShopItem` 的名称、数量、图标序列化引用均已写入 Prefab。
- 已检查本次 C# 文件未新增 `transform.Find`、`GetComponentsInChildren` 等 UI 节点查找。
- 未执行完整 Play Mode 页签点击与视觉回归，运行时交互仍需在可进入商店流程时确认。
