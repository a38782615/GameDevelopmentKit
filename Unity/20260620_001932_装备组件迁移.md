# 装备组件迁移

## 结果
- 新增 `EquipComponent`，用于承载装备属性变更数据。
- `AttributeComponent` 去掉了装备相关的 `AllModifiers` / `DataId`。
- 装备加成计算迁移到 `EquipComponentSystem`，装备穿戴/卸载/移除物品时会刷新属性。
- `UnitFactory` 创建单位时自动挂载 `EquipComponent` 并按当前背包状态初始化。

## 验证
- `AIBridgeCLI compile unity --raw --timeout 120000` 通过。
