# CodeStable 仓库接入

- 创建 `.codestable/attention.md`，作为每次会话必读的项目事实入口。
- 创建 `.codestable/lessons/`，用于按条目沉淀经验。
- 创建 `.codestable/work/`，用于保存活动中的跨会话任务文档。
- 仓库原先不存在 `.codestable/`，因此没有存量内容需要迁移或改写。
- 已确认 `.codestable/` 未被 `.gitignore` 忽略。
