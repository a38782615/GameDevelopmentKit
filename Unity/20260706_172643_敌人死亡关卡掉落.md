# 敌人死亡关卡掉落

## 修改内容

- 敌人死亡移除前读取当前关卡 `Stages.DropIds`。
- 通过 `RanDrawComponent.GetDropItem` 按权重抽取掉落 Id。
- 抽到的 Id 大于 0 时，显示掉落物实体 `UGFEntityId.Item10001`。
- `GFEntityDropItem` 改为 `GFEntityComponent` 子实体，并在显示时设置死亡位置。

## 验证

- 已执行空白检查，未发现行尾空白问题。
- 已检查本次修改的代码文件，无中文注释或中文字符串编码问题。
- 已通过 AIBridge 执行 Unity 编译，结果 `errorCount=0`、`warningCount=0`。

## 备注

- 本次未提交 git。
