# StageSubLevels查找按钮

## 改动

- `UIWidgetMapSystem` 改为先隐藏并解绑所有缓存按钮。
- 再遍历 `StageSubLevels`，根据 SubLevel 查找对应 `P{subLevel}` 的 `ExButton`。
- 找到按钮后再显示并绑定加载战斗事件，按钮显示完全由 `Stages` 当前 Level 的子关卡配置驱动。
- 移除不再需要的 `StageButtonSubLevels` 缓存字段。

## 验证

- 已通过 AIBridge 执行 Unity 编译检查。
- 编译结果：0 error，0 warning。
