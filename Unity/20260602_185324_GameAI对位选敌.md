## 本次任务

- 调整 `GameAIComponentSystem.Utility.FindNearestTarget` 的选敌逻辑。

## 修改内容

- 将原先按欧氏距离寻找最近敌人的逻辑，改为优先寻找敌方同 `PosIdx` 的目标。
- 如果当前对位没有可攻击目标，则按 `PosIdx + 1` 继续查找。
- 当阵位索引超过上限时，回绕到 `0` 号位继续查找。
- 仅返回存活的敌方单位，并保留最大距离过滤。

## 验证结果

- 已使用 AIBridge 执行 `focus --raw`。
- 已停止 Unity Play 模式。
- 已执行 `compile unity --raw --timeout 120000`，结果 `errorCount = 0`。
