# RanDraw 权重抽取

## 修改内容

- 在 `RanDrawComponentSystem.GetIdx` 中实现按权重比例抽取二维数组索引。
- 输入项格式按 `int[]` 的 `[0]=id`、`[1]=权重` 处理。
- 空数组、空项、字段不足、总权重小于等于 0 时返回 `-1`。
- `GetItem` 在未抽中有效索引时返回 `-1`，避免数组越界。

## 验证

- 已执行 `git diff --check`，未发现空白错误。
- 已通过 AIBridge 执行 Unity 编译检查，结果 `errorCount=0`、`warningCount=0`。
- 目标代码文件不包含中文注释或中文字符串，无中文编码问题。

## 备注

- 按用户要求，本次未提交 git。
