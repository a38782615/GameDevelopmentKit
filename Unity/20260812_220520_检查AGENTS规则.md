# 检查 AGENTS 规则

## 结果

- 项目级 AIBridge CLI 实际路径为 `.aibridge/cli/AIBridgeCLI.exe`，已修正 `AGENTS.md` 中残留的旧 `AIBridgeCache` 路径。
- `AGENTS.md` 已包含当前 Unity 版本、C# 9 约束和 AIBridge 工作流规则，无需增加其他项目规则。
- `Library/PackageCache` 下的 `AGENTS.md` 属于 AIBridge 包自身，读取时存在编码显示问题，未修改第三方文件。

## 注意

`AGENTS.md` 当前包含其他未提交改动，本次未覆盖、回滚或代为提交这些已有改动。
