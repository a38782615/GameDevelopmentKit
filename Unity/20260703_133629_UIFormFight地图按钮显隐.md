# UIFormFight地图按钮显隐

## 改动

- `UIFormFightSystem` 打开时根据 `FightComponent.CurrentMap` 刷新 `PreExButton`、`NextExButton` 显隐。
- 当前地图为第一张时隐藏 `Pre`，为最后一张时隐藏 `Next`，中间地图两者都显示。
- 加载地图子 UI 时增加 `Maps` 和 `CurrentMap` 边界保护，避免索引越界。

## 验证

- 已通过 AIBridge 执行 Unity 编译检查。
- 编译结果：0 error，0 warning。
