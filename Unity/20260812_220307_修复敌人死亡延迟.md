# 修复敌人死亡延迟

## 修改内容

- 将敌人死亡表现后的移除默认延迟从 1000ms 调整为 120ms。
- 将最小移除延迟从 300ms 调整为 120ms，保留短暂死亡动画，同时让 HP 归零的敌人及时从战场移除。
- 保留掉落生成和战斗胜利奖励在 `UnitComponent.Remove` 后执行，避免改变奖励判定顺序。

## 验证

- `dotnet build .\\Game.ET.Code.HotfixView.csproj --no-restore`：成功，0 个错误。
- Unity AIBridge CLI 未在当前工程目录找到，未能执行编辑器侧编译检查。
